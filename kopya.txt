$ loadkeys trq
$ setfont iso09.16
$ cfdisk
$ mkfs.ext4 /dev/sda1
$ mkfs.btrfs /dev/sda2
$ mount /dev/sda2 /mnt
$ mkdir /mnt/boot
$ mount /dev/sda1 /mnt/boot
$ pacstrap -i /mnt base base-devel linux-zen nano linux-firmware
$ pacstrap /mnt grub os-prober
$ genfstab -p /mnt >> /mnt/etc/fstab
$ arch-chroot /mnt
$ echo navi > /etc/hostname
$ pacman -S networkmanager
$ systemctl enable NetworkManager.service

$ nano /etc/vconsole.conf


-> KEYMAP=trq 
$ nano /etc/locale.conf

-> LANG=tr_TR.UTF-8 

$ ln -sf /usr/share/zoneinfo/Europe/Istanbul /etc/localtime

$ nano /etc/locale.gen

$ locale-gen


$ mkinitcpio -p linux-zen


$ grub-install --recheck /dev/sda


$ grub-mkconfig -o /boot/grub/grub.cfg


$ passwd


$ umount /mnt/boot

$ umount /mnt


$ reboot


$ pacman -S git


$ git clone https://github.com/Spica-Suzuki/arch-i3

#swap

$ Su


$ pacman -S fallocate


$ touch /swapfile


$ chattr +C /swapfile


$ fallocate -l 4G /swapfile


$ chmod 0600 /swapfile



$ mkswap /swapfile


$ swapon /swapfile


$ sudo nano /etc/fstab

-> burada diğer iki devreyi göreceksiniz. bunların aşağısına

/swapfile none swap sw 0 0 

